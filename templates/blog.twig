{% extends 'base.twig' %}
{% block title %}Blog{% endblock %}
{% block body %}

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

<hr>
    <div class="row">
        {% for blogitem in blog %}
            <h4>{{ blogitem.identifiant }}</h4>
            <p>{{ blogitem.message }}</p>
            <p style="font-style: italic;">Le {{ blogitem.date | date('d-m-Y') }}</p>
        {% endfor %}

    </div>
{% endblock %}